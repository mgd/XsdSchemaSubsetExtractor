# Schema Input Directory

Place the XSD schema files you want to extract subsets from in this directory, then configure
the extractor's `schemaDir` to point here.

For example schemas that demonstrate all extractor features, see [examples/schemas/](../examples/schemas/).
