pluginManagement {
    repositories {
        maven {
            url = uri("${settingsDir}/../../plugin/build/repo")
        }
        gradlePluginPortal()
    }
}
rootProject.name = 'schema-extractor-example'
